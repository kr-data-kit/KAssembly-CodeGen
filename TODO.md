# 중요 순서

- key를 쓰지 않고 사용가능하게 바꾸기 (빠르게 바꾸기)
=> 바꾸긴 했는데, `ALLBILL`처럼 버전이 아래인 것을 못 불러오는 문제가 있음

전체적으로 수정 : service에서 spec을 불러오는게 아닌, summary 리스트들 가져오기 동작 다음에 spec을 가져오는 동작을 사용해서 버전이 아래인 것이 detect 되었다면 summary를 더 추가해야함

map 쓰기 싫지만 편리함을위해 어쩔 수 없는 듯

- bulk update (xlsx, json)

- service 구조체가 값이 빠져도 gen이 가능하게

- 패키지 이름 바꾸기 (codegen)

- go.mod 추가 여부 설정 (+ 레포 생성 여부)

- README 다듬기

# Future


- 탬플릿에서 ast로 변환하기 : 보안 관련, 급선무가 아님

- 더 나은 명명 (en-name): 초반에 추가했으나, 중요한 API는 이름이 잘 구분되어있고, en 생성 퀄리티가 낮아서 나중에 다시 하기로

- 카테고리화 : 나중에