"""API endpoint modules.

Usage:
    from endpoints import <api endpoint module>
    
    # Example with specific endpoint
    {{- if .Services }}
    {{- $first := index .Services 0 }}
    params = {{ ToLowerCase $first.ResponseKey }}.Params(...)
    response = client.fetch({{ ToLowerCase $first.ResponseKey }}.ENDPOINT, params, {{ ToLowerCase $first.ResponseKey }}.Response)
    # or use the convenience function
    response = {{ ToLowerCase $first.ResponseKey }}.fetch(client, params)
    {{- end }}
"""

# Code generated by OpenAssembly-Binder. DO NOT EDIT.
# {{.RepositoryURL}}

# Endpoint modules are imported as namespaces
from . import (
    {{- range $i, $svc := .Services }}
    {{ ToLowerCase $svc.ResponseKey }},
    {{- end }}
)

__all__ = [
    {{- range $svc := .Services }}
    "{{ ToLowerCase $svc.ResponseKey }}",
    {{- end }}
]
