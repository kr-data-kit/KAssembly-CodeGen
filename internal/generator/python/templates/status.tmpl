"""API status codes for Open Assembly API."""

# Code generated by OpenAssembly-Binder. DO NOT EDIT.
# {{.RepositoryURL}}


from __future__ import annotations

from enum import Enum


__all__ = ["APIStatus"]


class APIStatus(Enum):
    """Status codes returned by the Open Assembly API."""
    
    # Success
    OK = ("INFO-000", "정상 처리되었습니다.")
    
    # Informational
    NO_DATA = ("INFO-200", "해당하는 데이터가 없습니다.")
    AUTH_RESTRICTED = ("INFO-300", "관리자에 의해 인증키 사용이 제한되었습니다.")
    
    # Client errors
    MISSING_REQUIRED = ("ERROR-300", "필수 값이 누락되어 있습니다. 요청인자를 참고 하십시오.")
    INVALID_AUTH = ("ERROR-290", "인증키가 유효하지 않습니다. 인증키가 없는 경우, 홈페이지에서 인증키를 신청하십시오.")
    SERVICE_NOT_FOUND = ("ERROR-310", "해당하는 서비스를 찾을 수 없습니다. 요청인자 중 SERVICE를 확인하십시오.")
    INVALID_LOCATION = ("ERROR-333", "요청위치 값의 타입이 유효하지 않습니다. 요청위치 값은 정수를 입력하세요.")
    DATA_LIMIT_EXCEEDED = ("ERROR-336", "데이터요청은 한번에 최대 1,000건을 넘을 수 없습니다.")
    TRAFFIC_EXCEEDED = ("ERROR-337", "일별 트래픽 제한을 넘은 호출입니다. 오늘은 더이상 호출할 수 없습니다.")
    
    # Server errors
    SERVER_ERROR = ("ERROR-500", "서버 오류입니다. 지속적으로 발생시 홈페이지로 문의(Q&A) 바랍니다.")
    DATABASE_ERROR = ("ERROR-600", "데이터베이스 연결 오류입니다. 지속적으로 발생시 홈페이지로 문의(Q&A) 바랍니다.")
    SQL_ERROR = ("ERROR-601", "SQL 문장 오류 입니다. 지속적으로 발생시 홈페이지로 문의(Q&A) 바랍니다.")
    CERTIFICATE_REVOKED = ("ERROR-990", "인증서가 폐기되었습니다. 홈페이지에서 인증키를 확인하십시오.")
    
    def __init__(self, code: str, description: str) -> None:
        self.code = code
        self.description = description
    
    @classmethod
    def from_code(cls, code: str) -> APIStatus:
        """Get APIStatus from status code string.
        
        Args:
            code: Status code string (e.g., "INFO-000")
            
        Returns:
            Corresponding APIStatus enum member
            
        Raises:
            ValueError: If code is not recognized
        """
        for status in cls:
            if status.code == code:
                return status
        raise ValueError(f"Unknown status code: {code}")
    
    def __str__(self) -> str:
        return self.code
    
    @property
    def is_success(self) -> bool:
        """Check if status indicates success."""
        return self == APIStatus.OK
    
    @property
    def is_error(self) -> bool:
        """Check if status indicates an error."""
        return self.code.startswith("ERROR")
